"""Agent evaluation: scripted requests against a real Prometheus and a real model.

Not part of the test suite — it needs an LLM and costs tokens. Run it with
``uv run python -m evals``; see ``docs/evals.md``.
"""
